# audience/

Notes on who specific work is *for* — readers, users, clients. Separate from `me/` because the audience's needs aren't the user's own preferences.

Populated by `/ingest-resource` (when a resource is primarily about an audience) and manually.

For seekers of truth