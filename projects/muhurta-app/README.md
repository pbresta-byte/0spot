# Muhūrta Gauge → PL0

Pointer entry — the real project lives elsewhere. Do not duplicate code here.

**Active project (current, canonical):** `C:\Users\drrag\OneDrive\Desktop\pl0-app\` — the consolidated Jyotish app, referred to as **PL0**. Its own README states it "supersedes the separate 'muhurta gauge' line — all its engines are merged here." Single-file HTML/CSS/JS app (`www/index.html`), no build step. GitHub repo: `https://github.com/pbresta-byte/pl0-app` (branch `main`), with CI (`.github/workflows/android-apk.yml`, `pages.yml`) building the APK and deploying Pages on push.

**Predecessor (superseded, kept for reference):** `E:\ClaudeWorkSpaceAnthropic\muhurta-app\` — the original SBC + Pañcha Pakshi analyzer this OS was first built around. Still live at `https://pbresta-byte.github.io/muhurta-gauge/` and `https://github.com/pbresta-byte/muhurta-gauge`, but no longer the app to extend — treat as historical/reference only. Its engine notes (SBC scoring, Pañcha Pakshi bird-states) still hold, since PL0 inherited them.

- This folder also holds `pl0-current-structure.canvas` (Obsidian canvas mapping PL0's live 14-page nav for redesign work) and `math-solar-position-sunrise-sunset.md` (derivation of the sunrise/sunset formula both apps share).
- Referenced elsewhere in this OS as **PL0** — e.g. the Jyotish panel of `/ask-the-board` (in `oanda-command-center`) draws on PL0's live Currency Significators when available.
- Source library for grounding/extending this app's rules: `knowledge/raw/2026-08-19-jyotish-library-catalog.md` — 1047 classical Jyotish texts, filename catalog only (not full-text ingested).
- **Location caveat:** there are 3 clones of this app on disk; the Desktop one above is canonical (has the real git remote + CI). See memory `project_pl0_app.md` in the Claude Code memory store if a future session needs the full disambiguation.

Update this file if the active project's location or public links change.
